const API_URL = process.env.API_URL || 'http://geography.lh/api';

export const CITY_URL = `${API_URL}/city`;
export const MOUNTAIN_URL = `${API_URL}/mountain`;
export const RIVER_URL = `${API_URL}/river`;
